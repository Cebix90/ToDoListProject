<Page x:Class="ToDoList.WorkTasksPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:ToDoList"
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800"
      Title="WorkTasksPage">

    <Grid Height="450">

        <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center">
            <TextBlock>Your current tasks:</TextBlock>
            <ScrollViewer Height="200" Width="400" Margin="0 10">
                <ItemsControl ItemsSource="{Binding WorkTaskList}" Margin ="10">
                    <ItemsControl.ItemTemplate>
                        <DataTemplate>
                            <local:WorkTask/>
                        </DataTemplate>
                    </ItemsControl.ItemTemplate>
                </ItemsControl>
            </ScrollViewer>

            <TextBlock Style="{StaticResource DefaultTextStyle}">Task Title</TextBlock>
            <TextBox Text="{Binding NewWorkTaskTitle, Mode=TwoWay}"
                     Style="{StaticResource DefaultBoxStyle}"></TextBox>
            <TextBlock Style="{StaticResource DefaultTextStyle}">Task Description</TextBlock>
            <TextBox Text="{Binding NewWorkTaskDescription, Mode=TwoWay}"
                     Style="{StaticResource DefaultBoxStyle}"></TextBox>
            <Grid>
                
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="*" />
                </Grid.ColumnDefinitions>

                <Button Command="{Binding AddNewTaskCommand}"
                        Style="{StaticResource DefaultButtonStyle}"
                        Content="Add"></Button>
                <Button Command="{Binding DeleteSelectedTasksCommand}"
                        Grid.Column="1"
                        Style="{StaticResource DefaultButtonStyle}"
                        Content="Remove"></Button>
            </Grid>
        </StackPanel>

        
    </Grid>
</Page>
